{
  "name": "troyprog-hugo",
  "version": "1.0.0",
  "description": "Hugo version of TroyProg blog",
  "scripts": {
    "postinstall": "echo 'Dependencies installed. Use ./bin/dev to start development server.'",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "test:e2e": "start-server-and-test 'hugo serve --port 8000' http://localhost:8000 cy:open",
    "test:e2e:ci": "start-server-and-test 'hugo serve --port 8000' http://localhost:8000 cy:run"
  },
  "dependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.9",
    "postcss-cli": "^11.0.1"
  },
  "devDependencies": {
    "cypress": "^14.5.1",
    "start-server-and-test": "^3.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
